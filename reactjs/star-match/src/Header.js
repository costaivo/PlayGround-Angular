
import './App.css';

const  Header = (props) => {
    return (
      <div className="help">
        Pick 1 or more numbers that sum to the number of stars
      </div>
    );
  };

export default Header